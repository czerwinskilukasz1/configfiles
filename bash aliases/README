Steps:

1. Paste text from .bashrc at the end of your .bashrc file.
2. Copy .my_bash_aliases to your profile directory.
3. Restart shell (run /bin/bash or log out and log in).


File experimental_aliases contains optional aliases that might or might not work, but still could be useful.

